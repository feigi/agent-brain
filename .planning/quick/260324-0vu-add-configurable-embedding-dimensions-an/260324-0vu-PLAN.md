---
phase: quick
plan: 260324-0vu
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - src/db/schema.ts
  - src/providers/embedding/ollama.ts
  - src/providers/embedding/mock.ts
  - src/providers/embedding/titan.ts
  - src/providers/embedding/index.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "EMBEDDING_DIMENSIONS env var controls vector dimensions for all providers"
    - "Ollama provider calls /api/embeddings and returns vectors of configured dimensions"
    - "Mock provider respects configured dimensions instead of hardcoded 512"
    - "Schema uses configurable dimensions from config instead of hardcoded 512"
  artifacts:
    - path: "src/providers/embedding/ollama.ts"
      provides: "Ollama embedding provider using native fetch"
    - path: "src/config.ts"
      provides: "embeddingDimensions, ollamaBaseUrl, ollamaModel config fields"
  key_links:
    - from: "src/db/schema.ts"
      to: "src/config.ts"
      via: "config.embeddingDimensions for vector column dimensions"
    - from: "src/providers/embedding/ollama.ts"
      to: "src/config.ts"
      via: "ollamaBaseUrl, ollamaModel for HTTP calls"
    - from: "src/providers/embedding/index.ts"
      to: "src/providers/embedding/ollama.ts"
      via: "factory case for ollama provider"
---

<objective>
Add configurable embedding dimensions and Ollama provider support for local deployment.

Purpose: Enable local development without AWS credentials by supporting Ollama as an embedding provider, and make embedding dimensions configurable so any model (Titan 512d, nomic-embed-text 768d, etc.) works.
Output: Updated config, schema, mock/titan providers using configurable dimensions, new Ollama provider, updated factory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/config.ts
@src/db/schema.ts
@src/providers/embedding/types.ts
@src/providers/embedding/mock.ts
@src/providers/embedding/titan.ts
@src/providers/embedding/index.ts
@src/utils/errors.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/providers/embedding/types.ts:

```typescript
export interface EmbeddingProvider {
  embed(text: string): Promise<number[]>;
  readonly modelName: string;
  readonly dimensions: number;
}
```

From src/utils/errors.ts:

```typescript
export class EmbeddingError extends DomainError {
  constructor(message: string) {
    super(message, "EMBEDDING_ERROR", 502);
  }
}
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add config fields and make dimensions configurable across config, schema, and existing providers</name>
  <files>src/config.ts, src/db/schema.ts, src/providers/embedding/mock.ts, src/providers/embedding/titan.ts</files>
  <action>
1. Update src/config.ts -- add three new fields:
   - `embeddingDimensions: Number(process.env.EMBEDDING_DIMENSIONS ?? "512")` -- default 512 preserves current behavior
   - `ollamaBaseUrl: process.env.OLLAMA_BASE_URL ?? "http://localhost:11434"` -- standard Ollama default
   - `ollamaModel: process.env.OLLAMA_MODEL ?? "nomic-embed-text"` -- recommended local model (768d)
   - Update the `embeddingProvider` type union to include `"ollama"`: `as "titan" | "mock" | "ollama"`

2. Update src/db/schema.ts -- replace hardcoded `{ dimensions: 512 }` with `{ dimensions: config.embeddingDimensions }`. Import config at top of file. This means the schema is evaluated at module load time using the configured dimensions.

3. Update src/providers/embedding/mock.ts -- change MockEmbeddingProvider to accept dimensions in constructor:
   - Constructor: `constructor(private readonly dims: number = 512)`
   - Change `readonly dimensions` to a getter: `get dimensions() { return this.dims; }`
   - Update the Array.from length to use `this.dims` instead of hardcoded 512

4. Update src/providers/embedding/titan.ts -- make TitanEmbeddingProvider accept dimensions in constructor:
   - Add `dims` parameter: `constructor(region = "us-east-1", timeoutMs = 10_000, dims = 512)`
   - Change `readonly dimensions = 512` to a getter: `get dimensions() { return this.dims; }`
   - Use `this.dims` in the InvokeModelCommand body instead of `this.dimensions` (to avoid the getter in the constructor timing issue -- just use `this.dims` directly)
     </action>
     <verify>
     <automated>npx tsx -e "import { config } from './src/config.js'; console.log('dims:', config.embeddingDimensions, 'ollamaUrl:', config.ollamaBaseUrl, 'ollamaModel:', config.ollamaModel); console.log('provider type includes ollama:', config.embeddingProvider !== undefined)" 2>&1 | head -5</automated>
     </verify>
     <done>config.ts has embeddingDimensions/ollamaBaseUrl/ollamaModel fields, schema.ts uses config.embeddingDimensions, MockEmbeddingProvider and TitanEmbeddingProvider accept configurable dimensions</done>
     </task>

<task type="auto">
  <name>Task 2: Create Ollama provider and update factory</name>
  <files>src/providers/embedding/ollama.ts, src/providers/embedding/index.ts</files>
  <action>
1. Create src/providers/embedding/ollama.ts:
   - Class `OllamaEmbeddingProvider` implements `EmbeddingProvider`
   - Constructor: `constructor(private readonly baseUrl: string, private readonly model: string, private readonly dims: number)`
   - `get modelName()` returns `"ollama:" + this.model` (e.g., "ollama:nomic-embed-text")
   - `get dimensions()` returns `this.dims`
   - `async embed(text: string): Promise<number[]>`:
     - Use native `fetch` (zero dependencies) to POST to `${this.baseUrl}/api/embeddings`
     - Request body: `{ model: this.model, prompt: text }`
     - Parse JSON response, extract `response.embedding` (array of numbers)
     - Validate returned vector length matches `this.dims`. If mismatch, throw `EmbeddingError` with message: `Ollama model ${this.model} returned ${actual}d vector, expected ${this.dims}d. Set EMBEDDING_DIMENSIONS=${actual} to match.`
     - Wrap fetch errors in `EmbeddingError`: catch block should throw `new EmbeddingError("Ollama embedding failed: " + message)`. Include a hint if the error looks like a connection refusal: append ` -- is Ollama running at ${this.baseUrl}?`
   - Import `EmbeddingError` from `../../utils/errors.js`

2. Update src/providers/embedding/index.ts:
   - Import `OllamaEmbeddingProvider` from `./ollama.js`
   - Add case `"ollama"` in createEmbeddingProvider switch:
     ```
     case "ollama":
       return new OllamaEmbeddingProvider(config.ollamaBaseUrl, config.ollamaModel, config.embeddingDimensions);
     ```
   - Pass `config.embeddingDimensions` to MockEmbeddingProvider and TitanEmbeddingProvider constructors in their existing cases:
     - `case "mock": return new MockEmbeddingProvider(config.embeddingDimensions);`
     - `case "titan": return new TitanEmbeddingProvider(config.awsRegion, config.embeddingTimeoutMs, config.embeddingDimensions);`
   - Add export: `export { OllamaEmbeddingProvider } from "./ollama.js";`
     </action>
     <verify>
     <automated>npx tsx -e "import { OllamaEmbeddingProvider } from './src/providers/embedding/ollama.js'; const p = new OllamaEmbeddingProvider('http://localhost:11434', 'nomic-embed-text', 768); console.log('model:', p.modelName, 'dims:', p.dimensions); console.log('OK')" 2>&1 | head -5</automated>
     </verify>
     <done>OllamaEmbeddingProvider exists, calls /api/embeddings via native fetch, validates dimension mismatch, factory creates all three provider types with configurable dimensions</done>
     </task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for Ollama provider and configurable dimensions</name>
  <files>src/providers/embedding/ollama.test.ts</files>
  <behavior>
    - Test: OllamaEmbeddingProvider reports correct modelName ("ollama:nomic-embed-text")
    - Test: OllamaEmbeddingProvider reports correct dimensions from constructor
    - Test: embed() calls correct URL and returns embedding from response
    - Test: embed() throws EmbeddingError when dimension mismatch (mock fetch returning wrong-size vector)
    - Test: embed() throws EmbeddingError with connection hint on fetch failure
    - Test: MockEmbeddingProvider respects custom dimensions (e.g., 768 produces 768-length vector)
  </behavior>
  <action>
Create src/providers/embedding/ollama.test.ts using vitest:
- Import `describe, it, expect, vi` from vitest
- Import `OllamaEmbeddingProvider` and `MockEmbeddingProvider`
- Use `vi.stubGlobal('fetch', ...)` to mock native fetch for Ollama tests
- Test modelName and dimensions properties
- Test successful embed: mock fetch to return `{ embedding: Array(768).fill(0.1) }`, verify result is the array and fetch was called with correct URL/body
- Test dimension mismatch: mock fetch returning 512d vector when provider expects 768d, expect EmbeddingError with helpful message
- Test connection error: mock fetch to reject with TypeError (like connection refused), expect EmbeddingError with "is Ollama running" hint
- Test MockEmbeddingProvider with dimensions=768: `new MockEmbeddingProvider(768)`, verify `embed("test")` returns 768-length array and `provider.dimensions === 768`
- Clean up global fetch mock with `vi.restoreAllMocks()` in afterEach
  </action>
  <verify>
    <automated>npx vitest run src/providers/embedding/ollama.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All tests pass: Ollama provider correctly calls API, validates dimensions, handles errors; MockEmbeddingProvider respects configurable dimensions</done>
</task>

</tasks>

<verification>
- `npx tsx -e "import { config } from './src/config.js'; console.log(config.embeddingDimensions, config.ollamaBaseUrl, config.ollamaModel)"` prints "512 http://localhost:11434 nomic-embed-text"
- `npx tsx -e "import { createEmbeddingProvider } from './src/providers/embedding/index.js'; const p = createEmbeddingProvider(); console.log(p.modelName, p.dimensions)"` prints "mock-deterministic 512" (default config)
- `EMBEDDING_DIMENSIONS=768 npx tsx -e "import { MockEmbeddingProvider } from './src/providers/embedding/mock.js'; const p = new MockEmbeddingProvider(768); const v = await p.embed('test'); console.log(v.length)"` prints "768"
- `npx vitest run src/providers/embedding/ollama.test.ts` passes all tests
</verification>

<success_criteria>

- config.ts exposes embeddingDimensions (default 512), ollamaBaseUrl, ollamaModel
- schema.ts vector column uses config.embeddingDimensions instead of hardcoded 512
- MockEmbeddingProvider and TitanEmbeddingProvider accept configurable dimensions
- OllamaEmbeddingProvider exists with zero external dependencies, calls /api/embeddings, validates dimension match
- Factory creates all three provider types with config-driven dimensions
- Unit tests pass for Ollama provider and configurable mock dimensions
  </success_criteria>

<output>
After completion, create `.planning/quick/260324-0vu-add-configurable-embedding-dimensions-an/260324-0vu-SUMMARY.md`
</output>
