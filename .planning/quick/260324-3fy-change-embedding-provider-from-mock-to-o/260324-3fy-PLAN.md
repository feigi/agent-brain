---
phase: quick-260324-3fy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.env]
autonomous: true
requirements: [quick-260324-3fy]
must_haves:
  truths:
    - "EMBEDDING_PROVIDER is set to ollama in .env"
  artifacts:
    - path: ".env"
      provides: "Local environment configuration"
      contains: "EMBEDDING_PROVIDER=ollama"
  key_links: []
---

<objective>
Change EMBEDDING_PROVIDER from `mock` to `ollama` in .env so the dev server uses the local Ollama embedding provider.

Purpose: Switch from the no-op mock provider to real Ollama embeddings for local development.
Output: Updated .env with EMBEDDING_PROVIDER=ollama.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set EMBEDDING_PROVIDER=ollama in .env</name>
  <files>.env</files>
  <action>
    In .env, change the line:

      EMBEDDING_PROVIDER=mock

    to:

      EMBEDDING_PROVIDER=ollama

    Also update the comment above it to reflect the available options. The current comment says `# Embedding provider: titan | mock` — change it to `# Embedding provider: titan | ollama | mock`.

    Do not touch any other lines.

  </action>
  <verify>
    <automated>grep 'EMBEDDING_PROVIDER=ollama' /Users/chris/dev/agent-brain/.env</automated>
  </verify>
  <done>EMBEDDING_PROVIDER=ollama appears in .env and the comment lists ollama as an option.</done>
</task>

</tasks>

<verification>
Run: `grep 'EMBEDDING_PROVIDER' .env`
Expected output: `EMBEDDING_PROVIDER=ollama`
</verification>

<success_criteria>
.env contains EMBEDDING_PROVIDER=ollama. Starting the MCP server will use the Ollama embedding provider instead of the mock.
</success_criteria>

<output>
After completion, create `.planning/quick/260324-3fy-change-embedding-provider-from-mock-to-o/260324-3fy-SUMMARY.md`
</output>
