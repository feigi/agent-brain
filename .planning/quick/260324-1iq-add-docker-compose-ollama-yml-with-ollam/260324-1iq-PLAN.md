---
phase: quick
plan: 260324-1iq
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.ollama.yml
  - .env.example
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "docker compose -f docker-compose.yml -f docker-compose.ollama.yml up starts Postgres + Ollama with nomic-embed-text pulled"
    - "README documents mock, Ollama, and Titan embedding modes with clear setup steps"
    - ".env.example includes Ollama-related variables"
  artifacts:
    - path: "docker-compose.ollama.yml"
      provides: "Ollama service definition extending base compose"
    - path: "README.md"
      provides: "Local development section covering all embedding modes"
    - path: ".env.example"
      provides: "Complete env var reference including Ollama vars"
  key_links:
    - from: "docker-compose.ollama.yml"
      to: "docker-compose.yml"
      via: "extends base postgres service"
    - from: "docker-compose.ollama.yml"
      to: "src/config.ts"
      via: "env vars EMBEDDING_PROVIDER, EMBEDDING_DIMENSIONS, OLLAMA_BASE_URL match config.ts expectations"
---

<objective>
Add docker-compose.ollama.yml with Ollama service for real local embeddings, update .env.example with Ollama vars, and expand README.md with a local development section covering mock, Ollama, and Titan modes.

Purpose: Let developers run real semantic search locally without AWS credentials by using Ollama with nomic-embed-text.
Output: docker-compose.ollama.yml, updated .env.example, updated README.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docker-compose.yml
@src/config.ts
@src/providers/embedding/ollama.ts
@README.md
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docker-compose.ollama.yml and update .env.example</name>
  <files>docker-compose.ollama.yml, .env.example</files>
  <action>
Create `docker-compose.ollama.yml` as an override file designed to be used with: `docker compose -f docker-compose.yml -f docker-compose.ollama.yml up`

The file should define:
1. An `ollama` service using image `ollama/ollama:latest`
   - Expose port 11434:11434
   - Mount a named volume `ollama_data` to `/root/.ollama` for model persistence
   - Add a healthcheck: `ollama list` with interval 10s, timeout 5s, retries 5
   - Add an entrypoint script (inline via command) that:
     a. Starts `ollama serve` in the background
     b. Waits for Ollama to be ready (curl retry loop on localhost:11434)
     c. Pulls `nomic-embed-text` model
     d. Waits on the serve process (keeps container running)

2. An `ollama-init` service (or handle in the ollama service entrypoint) is NOT needed -- just do the pull in the ollama service entrypoint as described above.

3. Include the `postgres` service reference (no overrides needed, inherits from base).

4. Add named volume `ollama_data`.

5. Set environment variables on the ollama service: none needed (Ollama uses no env config).

The file should NOT set environment variables for the agentic-brain app -- those go in .env. The compose file only defines infrastructure services.

Update `.env.example` to add Ollama-related variables after the existing content:

```
# Embedding provider: titan | mock | ollama
EMBEDDING_PROVIDER=mock

# Ollama (only needed when EMBEDDING_PROVIDER=ollama)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=nomic-embed-text
EMBEDDING_DIMENSIONS=768
```

Note: Update the existing EMBEDDING_PROVIDER comment to include `ollama` as an option. Keep all existing variables. Add the Ollama block after the AWS block.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.ollama.yml config --quiet 2>&1; echo "exit: $?"</automated>
  </verify>
  <done>docker-compose.ollama.yml exists and validates with base compose file. .env.example includes Ollama variables with correct defaults matching src/config.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md with local development embedding modes</name>
  <files>README.md</files>
  <action>
Update README.md with expanded local development documentation. Make these changes:

1. In the "Configure" section (### 2. Configure), update the env block to show `ollama` as an option:
   ```
   # Embedding provider: "mock" for local dev, "ollama" for local real embeddings, "titan" for production
   EMBEDDING_PROVIDER=mock
   ```

2. Add a new section after "### 5. Use the MCP Inspector" and before "---" (before the MCP Tools reference section). Title it `### Embedding providers`. Content:

   **Mock (default)** -- Returns random vectors. Good for testing tool behavior without real embeddings. No setup needed.
   ```bash
   EMBEDDING_PROVIDER=mock
   ```

   **Ollama (local real embeddings)** -- Runs nomic-embed-text locally via Ollama. Real semantic search without AWS credentials.
   ```bash
   # Start Postgres + Ollama (pulls nomic-embed-text on first run)
   docker compose -f docker-compose.yml -f docker-compose.ollama.yml up -d

   # Configure
   EMBEDDING_PROVIDER=ollama
   EMBEDDING_DIMENSIONS=768
   OLLAMA_BASE_URL=http://localhost:11434
   OLLAMA_MODEL=nomic-embed-text
   ```
   First startup takes a few minutes to download the model (~274MB).

   **Titan V2 (production)** -- Amazon Titan Text Embeddings V2 via Bedrock. Requires AWS credentials.
   ```bash
   EMBEDDING_PROVIDER=titan
   EMBEDDING_DIMENSIONS=512
   AWS_REGION=us-east-1
   ```

   Add a note: "When switching between providers with different dimensions, you must re-embed existing memories or start with a fresh database, as vector dimensions are fixed at index creation time."

3. In the "Configuration reference" table at the bottom, add these rows:
   - `EMBEDDING_DIMENSIONS` | `512` | Vector dimensions (512 for Titan, 768 for nomic-embed-text)
   - `OLLAMA_BASE_URL` | `http://localhost:11434` | Ollama API endpoint
   - `OLLAMA_MODEL` | `nomic-embed-text` | Ollama model for embeddings

4. In the Providers section of the architecture diagram, update:
   ```
   │  ├─ Amazon Titan V2      │
   │  ├─ Ollama (local)        │
   │  └─ Mock (dev/test)      │
   ```
  </action>
  <verify>
    <automated>grep -c "ollama" README.md | xargs -I{} test {} -ge 5 && echo "PASS: ollama mentioned 5+ times" || echo "FAIL: ollama not sufficiently documented"</automated>
  </verify>
  <done>README.md documents all three embedding providers (mock, ollama, titan) with setup instructions, docker compose command for Ollama mode, and configuration reference table includes all Ollama-related env vars.</done>
</task>

</tasks>

<verification>
1. `docker compose -f docker-compose.yml -f docker-compose.ollama.yml config` validates without errors
2. README.md contains Ollama setup instructions with docker compose command
3. .env.example lists OLLAMA_BASE_URL, OLLAMA_MODEL, EMBEDDING_DIMENSIONS
4. All env var names in docker-compose.ollama.yml and README match src/config.ts exactly
</verification>

<success_criteria>
- docker-compose.ollama.yml defines ollama service with model pull on startup, healthcheck, and volume persistence
- .env.example updated with all Ollama-related variables
- README.md has clear section explaining mock vs Ollama vs Titan modes with copy-paste setup commands
- Env var names are consistent: EMBEDDING_PROVIDER, EMBEDDING_DIMENSIONS, OLLAMA_BASE_URL, OLLAMA_MODEL (matching src/config.ts)
</success_criteria>

<output>
After completion, create `.planning/quick/260324-1iq-add-docker-compose-ollama-yml-with-ollam/260324-1iq-SUMMARY.md`
</output>
