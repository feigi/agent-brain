---
phase: quick-260324-3sx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - docker-compose.ollama.yml
  - package.json
autonomous: true
requirements: [QUICK-3sx]

must_haves:
  truths:
    - "docker compose up starts both postgres and ollama services from a single file"
    - "docker-compose.ollama.yml no longer exists"
    - "npm run dev uses the single docker-compose.yml"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Combined postgres + ollama service definitions"
      contains: "ollama"
    - path: "package.json"
      provides: "Updated dev script without multi-file compose"
      contains: "docker compose up"
  key_links:
    - from: "package.json dev script"
      to: "docker-compose.yml"
      via: "docker compose up"
      pattern: "docker compose up"
---

<objective>
Merge docker-compose.ollama.yml into docker-compose.yml so there is a single compose file with both postgres and ollama services, then delete the ollama-specific file and update the dev script.

Purpose: Simplify local development -- one file, one command, no multi-file compose flags.
Output: Single docker-compose.yml with both services, updated package.json dev script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docker-compose.yml
@docker-compose.ollama.yml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge compose files and update dev script</name>
  <files>docker-compose.yml, package.json</files>
  <action>
Rewrite docker-compose.yml to contain both services inline (no extends):

1. Keep the existing postgres service exactly as-is (pgvector/pgvector:pg17, port 5432, env vars, volume mount for init-extensions.sql, healthcheck).

2. Add the ollama service below postgres with all properties inlined (not using extends):
   - image: ollama/ollama:latest
   - ports: "11434:11434"
   - volumes: ollama_data:/root/.ollama
   - healthcheck: test ["CMD", "ollama", "list"], interval 10s, timeout 5s, retries 5
   - entrypoint: ["/bin/sh", "-c"]
   - command: the multi-line script that runs `ollama serve &`, waits for ready, pulls nomic-embed-text, then `wait`

3. volumes section must declare both pgdata and ollama_data.

4. In package.json, update the "dev" script from:
   `docker compose -f docker-compose.yml -f docker-compose.ollama.yml up -d --wait && npx drizzle-kit migrate && EMBEDDING_PROVIDER=ollama EMBEDDING_DIMENSIONS=768 tsx watch src/server.ts`
   to:
   `docker compose up -d --wait && npx drizzle-kit migrate && EMBEDDING_PROVIDER=ollama EMBEDDING_DIMENSIONS=768 tsx watch src/server.ts`

   The only change is replacing `-f docker-compose.yml -f docker-compose.ollama.yml up` with just `up` (docker compose defaults to docker-compose.yml).
   </action>
   <verify>
   <automated>docker compose -f docker-compose.yml config --quiet 2>&1; echo "exit: $?"</automated>
   </verify>
   <done>docker-compose.yml contains both postgres and ollama services with all properties inlined. package.json dev script uses plain `docker compose up` without -f flags.</done>
   </task>

<task type="auto">
  <name>Task 2: Delete docker-compose.ollama.yml</name>
  <files>docker-compose.ollama.yml</files>
  <action>
Delete docker-compose.ollama.yml using `git rm docker-compose.ollama.yml`. This file is no longer needed since its contents have been merged into docker-compose.yml.

Also revert the uncommitted changes to docker-compose.ollama.yml first if any exist (git status showed it as modified), since we are deleting it entirely.
</action>
<verify>
<automated>test ! -f docker-compose.ollama.yml && echo "DELETED" || echo "STILL EXISTS"</automated>
</verify>
<done>docker-compose.ollama.yml is removed from the working tree and staged for deletion.</done>
</task>

</tasks>

<verification>
1. `docker compose config --quiet` exits 0 (valid compose file)
2. `docker-compose.ollama.yml` does not exist
3. `grep -c "ollama" docker-compose.yml` returns a positive count (ollama service present)
4. `grep "docker compose up" package.json` shows no `-f` flags
</verification>

<success_criteria>

- Single docker-compose.yml with postgres and ollama services
- docker-compose.ollama.yml deleted
- package.json dev script simplified to use default compose file
- `docker compose config` validates without errors
  </success_criteria>

<output>
After completion, create `.planning/quick/260324-3sx-merge-the-two-docker-compose-files/260324-3sx-SUMMARY.md`
</output>
