---
phase: quick
plan: 260329-28f
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/memory.ts
  - src/db/schema.ts
  - src/repositories/types.ts
  - src/repositories/memory-repository.ts
  - src/services/memory-service.ts
  - src/tools/memory-create.ts
  - src/tools/memory-search.ts
  - src/tools/memory-list.ts
  - src/tools/memory-list-recent.ts
  - src/tools/memory-session-start.ts
  - src/tools/memory-list-stale.ts
  - src/tools/memory-archive.ts
  - src/routes/api-schemas.ts
  - src/prompts/memory-guidance.ts
  - drizzle/0003_rename_project_to_workspace.sql
  - tests/integration/memory-scoping.test.ts
autonomous: true
must_haves:
  truths:
    - "Existing 'project' scope data is renamed to 'workspace' in database"
    - "New 'project' scope memories are visible across all workspaces (no project_id filter)"
    - "Project-scoped memories cannot be created by agents autonomously (source != agent-auto)"
    - "Search with project_id returns workspace + user scoped memories AND all project-scoped memories"
    - "memory_create with scope=workspace behaves exactly like old scope=project (scoped to project_id)"
  artifacts:
    - path: "drizzle/0003_rename_project_to_workspace.sql"
      provides: "Migration to rename project->workspace and add project scope"
    - path: "src/types/memory.ts"
      provides: "Updated MemoryScope type with workspace | user | project"
    - path: "src/db/schema.ts"
      provides: "Updated pgEnum with three scope values"
  key_links:
    - from: "src/tools/memory-create.ts"
      to: "src/services/memory-service.ts"
      via: "ValidationError when scope=project and source=agent-auto"
      pattern: "scope.*project.*agent-auto.*ValidationError"
    - from: "src/repositories/memory-repository.ts"
      to: "memories table"
      via: "search includes project-scoped memories regardless of project_id"
      pattern: "scope.*project.*OR"
---

<objective>
Rename existing `project` scope to `workspace` and add a new cross-workspace `project` scope.

Purpose: Enable project-wide memories visible across all workspaces (e.g., "always use ESM imports" applies to every workspace in the project). The old `project` scope behavior (scoped to project_id) becomes `workspace`. The new `project` scope has no project_id requirement and is user-confirmed only (no agent-auto creation).

Output: DB migration, updated types/schema, updated repository/service/tool/route logic, updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/types/memory.ts
@src/db/schema.ts
@src/repositories/types.ts
@src/repositories/memory-repository.ts
@src/services/memory-service.ts
@src/tools/memory-create.ts
@src/tools/memory-search.ts
@src/tools/memory-list.ts
@src/routes/api-schemas.ts
@src/routes/api-tools.ts
@src/prompts/memory-guidance.ts
@tests/integration/memory-scoping.test.ts

<interfaces>
<!-- Current scope type and enum that must be updated -->
From src/types/memory.ts:
```typescript
export type MemoryScope = "project" | "user";
// MemoryCreate.scope defaults to "project"
// MemoryCreate.project_id is required (string, not optional)
```

From src/db/schema.ts:

```typescript
export const memoryScopeEnum = pgEnum("memory_scope", ["project", "user"]);
// memories.project_id is NOT NULL with FK to projects.id
// memories.scope defaults to "project"
```

From src/repositories/types.ts:

```typescript
export interface SearchOptions {
  scope: "project" | "user" | "both";
  project_id: string;
  // ...
}
export interface ListOptions {
  scope: "project" | "user";
  project_id: string;
  // ...
}
```

From src/services/memory-service.ts:

```typescript
// canAccess: scope === "project" => true, else author check
// search: scope param is "project" | "user" | "both"
// create: scope defaults to "project", project_id required
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema, types, and migration -- rename project->workspace, add project scope</name>
  <files>
    src/types/memory.ts,
    src/db/schema.ts,
    src/repositories/types.ts,
    drizzle/0003_rename_project_to_workspace.sql
  </files>
  <action>
1. **src/types/memory.ts**: Change `MemoryScope` to `"workspace" | "user" | "project"`. Update `MemoryCreate`:
   - Change `scope` default comment from "project" to "workspace"
   - Make `project_id` optional (`string | undefined`) -- project-scoped memories do not require project_id. NOTE: only MemoryCreate.project_id becomes optional; the Memory interface keeps project_id as string (existing memories always have one, and workspace/user memories still require it).

2. **src/db/schema.ts**: Update `memoryScopeEnum` to `["workspace", "user", "project"]`. Change the default on `memories.scope` from `"project"` to `"workspace"`. Make `memories.project_id` nullable (remove `.notNull()`) since project-scoped memories may have null project_id. Keep the FK reference to projects.id.

3. **src/repositories/types.ts**: Update all scope literals:
   - `ListOptions.scope`: `"workspace" | "user" | "project"`
   - `SearchOptions.scope`: `"workspace" | "user" | "both"` (keep "both" for cross-scope search; "both" means workspace+user, search always includes project-scoped too)
   - `ListOptions.project_id`: make optional (`string | undefined`) for project-scope listing
   - `findDuplicates` scope param: `"workspace" | "user" | "project"`

4. **drizzle/0003_rename_project_to_workspace.sql**: Write a hand-crafted SQL migration (do NOT use drizzle-kit generate since we need custom enum rename logic):

   ```sql
   -- Rename 'project' to 'workspace' in the memory_scope enum, then add 'project' as new value
   -- Step 1: Add 'workspace' to enum
   ALTER TYPE memory_scope ADD VALUE IF NOT EXISTS 'workspace';
   -- Step 2: Update all existing 'project' rows to 'workspace'
   UPDATE memories SET scope = 'workspace' WHERE scope = 'project';
   -- Step 3: We cannot remove enum values in PostgreSQL, but we now have
   -- 'workspace', 'user', 'project' in the enum. The old 'project' value
   -- is now repurposed for cross-workspace scope. No rows should have scope='project'
   -- after the UPDATE above, so the semantic meaning is cleanly changed.

   -- Step 4: Make project_id nullable for project-scoped memories
   ALTER TABLE memories ALTER COLUMN project_id DROP NOT NULL;

   -- Step 5: Update default scope from 'project' to 'workspace'
   ALTER TABLE memories ALTER COLUMN scope SET DEFAULT 'workspace';
   ```

   IMPORTANT: PostgreSQL enums cannot have values removed. Since the old `project` value already exists in the enum, and we are repurposing it with a different semantic meaning, we just need to: (a) add `workspace`, (b) migrate existing data from `project` to `workspace`, (c) the existing `project` enum value is now the cross-workspace scope. This is clean because after the UPDATE, no rows have the old `project` meaning.
   </action>
   <verify>
   <automated>npx tsx -e "import { memoryScopeEnum } from './src/db/schema.js'; console.log(memoryScopeEnum.enumValues); import type { MemoryScope } from './src/types/memory.js'; const s: MemoryScope = 'workspace'; console.log('type check passed')" 2>&1 | grep -q "workspace" && echo "PASS" || echo "FAIL"</automated>
   </verify>
   <done> - MemoryScope type is "workspace" | "user" | "project" - DB schema enum has all three values - Migration SQL file exists and correctly renames data + adds workspace value - MemoryCreate.project_id is optional - Repository types updated with new scope values
   </done>
   </task>

<task type="auto">
  <name>Task 2: Repository, service, tools, routes -- implement new scope logic</name>
  <files>
    src/repositories/memory-repository.ts,
    src/services/memory-service.ts,
    src/tools/memory-create.ts,
    src/tools/memory-search.ts,
    src/tools/memory-list.ts,
    src/tools/memory-list-stale.ts,
    src/tools/memory-list-recent.ts,
    src/tools/memory-archive.ts,
    src/tools/memory-session-start.ts,
    src/routes/api-schemas.ts,
    src/routes/api-tools.ts,
    src/prompts/memory-guidance.ts
  </files>
  <action>
**Repository (memory-repository.ts):**

1. **search()**: Update scope conditions:
   - `scope === "workspace"`: filter by `eq(memories.project_id, options.project_id)` (same as old "project" behavior)
   - `scope === "user"`: unchanged (author + scope=user)
   - `scope === "both"`: workspace memories for project_id OR user's own user-scoped memories -- same as old behavior but use `eq(memories.scope, "workspace")` instead of checking project_id alone
   - **CRITICAL**: In ALL search modes, ALSO include project-scoped memories: add `or(eq(memories.scope, "project"))` to every branch. Project-scoped memories are always returned regardless of project_id filter. For "both", the OR becomes: `(project_id match AND scope=workspace) OR (author match AND scope=user) OR (scope=project)`.

2. **list()**: Update scope conditions:
   - `scope === "workspace"`: same as old project behavior (filter project_id)
   - `scope === "user"`: unchanged
   - `scope === "project"`: filter `eq(memories.scope, "project")` only -- no project_id filter. If project_id is provided, still no filter on it (project scope is cross-workspace).

3. **listRecentBothScopes()**: Add project-scoped memories to the OR condition: `or(eq(memories.project_id, options.project_id), and(eq(memories.author, options.user_id), eq(memories.scope, "user")), eq(memories.scope, "project"))`.

4. **findRecentActivity()**: Update the scope enforcement push at line ~452: replace `eq(memories.scope, "project")` with `or(eq(memories.scope, "workspace"), eq(memories.scope, "project"))` so both workspace and project-scoped memories are included.

5. **findDuplicates()**: Update scope checks: `scope === "workspace"` filters by projectId. `scope === "project"` checks all project-scoped memories (no project_id filter). `scope === "user"` checks against workspace + user scope (update literal from "project" to "workspace").

**Service (memory-service.ts):**

1. **canAccess()**: Update: `scope === "workspace"` or `scope === "project"` returns true (both are shared). `scope === "user"` checks author.

2. **create()**:
   - NEW GUARD (before existing guards): If `scope === "project"` and `source === "agent-auto"`, throw `ValidationError("Project-scoped memories require user confirmation and cannot be created autonomously (source: 'agent-auto').")`. This is the FIRST guard -- even before session validation.
   - For project scope: project_id is optional. If provided, still auto-create project. If not provided, skip project auto-creation.
   - Default scope changes from "project" to "workspace" (update the `?? "project"` to `?? "workspace"` throughout).
   - When building memoryData, use `input.project_id ?? null` for project_id field (since it is now nullable for project scope).

3. **search()**: The scope parameter type changes: `"project" | "user" | "both"` stays the same for the service method signature (callers still pass "project" meaning workspace-scoped search). WAIT -- actually, the scope param at the service level needs to match the new semantics. Change the service search signature scope to `"workspace" | "user" | "both"`. The repository handles including project-scope automatically.

4. **getWithComments()**: Update `can_comment` logic: `isProject` should check for `scope === "workspace" || scope === "project"` (both shared scopes allow comments from non-owners).

5. **addComment()**: Update the user-scope block comment check: `memory.scope === "user"` stays the same. No change needed here since workspace and project scopes both allow comments.

**Tools:**

1. **memory-create.ts**: Update scope enum to `z.enum(["workspace", "user", "project"])` with `.catch("workspace")`. Update description: `"'workspace' scopes to this workspace (shared with team), 'user' is private to you, 'project' is visible across all workspaces (user-confirmed only, not for agent-auto)"`. Make `project_id` optional with `.optional()` and update description to note it is required for workspace/user scope but optional for project scope.

2. **memory-search.ts**: Update scope enum to `z.enum(["workspace", "user", "both"]).catch("workspace")`. Update descriptions accordingly. Keep `project_id` as required (search always needs a workspace context even though it also returns project-scoped results).

3. **memory-list.ts**: Update scope enum to `z.enum(["workspace", "user", "project"]).catch("workspace")`. Make `project_id` optional (not needed for project-scope listing). Update descriptions.

4. **memory-list-stale.ts**: No scope change needed (it lists by project_id, which is workspace-level). Keep as-is but update any scope literal references if present.

5. **memory-list-recent.ts**: No scope change needed (it queries by project_id for activity). Keep as-is.

6. **memory-session-start.ts**: No scope change needed (it delegates to search with "both" scope).

7. **memory-archive.ts**: No scope change needed (works by ID, access control handles scope).

**Routes (api-schemas.ts):**

1. Update `scopeEnum` to `z.enum(["workspace", "user", "project"])`.
2. Update `memory_create` schema: scope default to `"workspace"`, project_id to optional.
3. Update `memory_search` schema: scope to `z.enum(["workspace", "user", "both"]).default("workspace")`.
4. Update `memory_list` schema: scope default to `"workspace"`, project_id optional.

**Routes (api-tools.ts):**

1. Update `memory_create` handler: pass `b.project_id` (which may now be undefined).
2. Update `memory_list` handler: project_id may be undefined.
3. No other route handler changes needed -- they pass through to service.

**Prompts (memory-guidance.ts):**

Add a section to MEMORY_GUIDANCE_TEXT about scopes:

```
### Memory Scopes

- **workspace** (default): Visible to all team members in this workspace. Requires project_id.
- **user**: Private to you, visible across all your workspaces.
- **project**: Visible to all users across ALL workspaces. Use for universal project knowledge (coding standards, architecture principles). Cannot be created autonomously -- requires user confirmation (source must not be 'agent-auto').
```

  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - TypeScript compiles with zero errors
    - memory_create rejects scope=project with source=agent-auto (ValidationError)
    - Search includes project-scoped memories in all search modes
    - Scope enum in all tool definitions uses workspace/user/project
    - Default scope is "workspace" everywhere
    - project_id is optional in create and list for project scope
    - Memory guidance prompt documents all three scopes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update and add integration tests for new scope behavior</name>
  <files>
    tests/integration/memory-scoping.test.ts,
    tests/integration/access-control.test.ts,
    tests/integration/duplicate-detection.test.ts
  </files>
  <behavior>
    - Test: workspace-scoped memory NOT visible in other workspace (same as old project test, but scope="workspace")
    - Test: project-scoped memory visible across workspaces (create in workspace-a, search from workspace-b finds it)
    - Test: project-scoped memory visible even without project_id in create (project_id=undefined)
    - Test: memory_create with scope=project and source=agent-auto throws ValidationError
    - Test: memory_create with scope=project and source=manual succeeds
    - Test: search with scope=both returns workspace + user + project-scoped memories
    - Test: default scope is "workspace" when not specified
    - Test: existing "user-scoped memory visible across projects" test still passes (renamed from project references to workspace where needed)
  </behavior>
  <action>
1. **tests/integration/memory-scoping.test.ts**:
   - Rename existing "project-scoped memory not visible in other project" test to "workspace-scoped memory not visible in other workspace" and change scope from "project" to "workspace".
   - Update "user-scoped memory visible across projects" -- the test body is fine but update any scope literals from "project" to "workspace" where they reference the old project scope.
   - Add new test: "project-scoped memory visible across all workspaces" -- create with scope="project", project_id=undefined (or any project_id), source="manual". Search from a different project_id and verify the project-scoped memory appears.
   - Add new test: "project-scoped memory cannot be created autonomously" -- create with scope="project", source="agent-auto", expect ValidationError.
   - Add new test: "project-scoped memory can be created manually" -- create with scope="project", source="manual", expect success.
   - Add new test: "search scope=both includes project-scoped memories" -- create workspace, user, and project memories. Search with scope="both" and verify all three appear.
   - Add new test: "default scope is workspace" -- create without explicit scope, verify memory.scope === "workspace".

2. **tests/integration/access-control.test.ts**: Update any scope="project" references to scope="workspace" where they test the old project-scope behavior. If tests reference project-scoped visibility, ensure they use "workspace".

3. **tests/integration/duplicate-detection.test.ts**: Update scope literals from "project" to "workspace" where they reference the old project-scope dedup behavior. Ensure project-scope dedup works (project-scoped memories check against all project-scoped memories, not workspace-scoped).

IMPORTANT: For project-scoped memory creation in tests, always provide source="manual" (not "agent-auto") since project scope requires user confirmation.
</action>
<verify>
<automated>cd /Users/chris/dev/agent-brain && npx vitest run tests/integration/memory-scoping.test.ts tests/integration/access-control.test.ts tests/integration/duplicate-detection.test.ts 2>&1 | tail -30</automated>
</verify>
<done> - All existing tests pass with updated scope values - New tests for project scope cross-workspace visibility pass - New test for agent-auto rejection on project scope passes - New test for search scope=both including project-scoped memories passes - Default scope test confirms "workspace"
</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero TypeScript errors
2. `npx vitest run` -- all tests pass (unit + integration)
3. Migration SQL is syntactically valid and idempotent-safe (ADD VALUE IF NOT EXISTS)
4. Manual check: creating a memory without scope results in scope="workspace"
</verification>

<success_criteria>

- DB migration renames all existing project-scoped rows to workspace
- Three-scope enum (workspace, user, project) works end-to-end
- Project-scoped memories are cross-workspace (no project_id filter in search/list)
- Agent-auto creation of project scope is rejected with clear error
- All 10+ integration tests pass
- TypeScript compiles cleanly
  </success_criteria>

<output>
After completion, create `.planning/quick/260329-28f-rename-scope-project-to-workspace-add-cr/260329-28f-SUMMARY.md`
</output>
