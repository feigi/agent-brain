---
phase: quick
plan: 260326-2e5
subsystem: server
tags: [crash-handling, connection-stability, docker, production]
key-files:
  created: []
  modified:
    - src/server.ts
    - src/db/index.ts
    - docker-compose.prod.yml
decisions:
  - "idle_timeout: 0 disables connection recycling -- appropriate for long-running server with bursty MCP traffic"
  - "connect_timeout: 10s provides explicit timeout instead of relying on OS default"
  - "restart: unless-stopped chosen over always -- allows manual docker stop without auto-restart"
metrics:
  duration: 1min
  completed: "2026-03-26"
  tasks: 2
  files: 3
---

# Quick Task 260326-2e5: Add Crash Handlers and Fix Connection Stability

Process crash visibility, route error handling, DB connection persistence, and Docker restart policy for production resilience.

## Commits

| #   | Hash    | Description                                              |
| --- | ------- | -------------------------------------------------------- |
| 1   | bd9efde | Add crash handlers and error handling to GET/DELETE /mcp |
| 2   | 83749fb | Fix DB idle timeout and add Docker restart policy        |

## Changes

### Task 1: Process crash handlers and route error handling (src/server.ts)

- Added `unhandledRejection` handler at top of `main()` -- logs reason before `process.exit(1)`
- Added `uncaughtException` handler at top of `main()` -- logs error before `process.exit(1)`
- Wrapped GET /mcp (SSE stream) handler in try/catch with `logger.error("MCP SSE error:", error)`
- Wrapped DELETE /mcp (session teardown) handler in try/catch with `logger.error("MCP session teardown error:", error)`
- Both catch blocks check `res.headersSent` before sending 500

### Task 2: DB idle timeout and Docker restart (src/db/index.ts, docker-compose.prod.yml)

- Changed `idle_timeout: 20` to `idle_timeout: 0` -- connections persist for pool lifetime, no idle disconnects
- Added `connect_timeout: 10` -- explicit 10s connection timeout
- Added `restart: unless-stopped` to agent-brain service in docker-compose.prod.yml

## Deviations from Plan

None -- plan executed exactly as written.

## Known Stubs

None.

## Self-Check: PASSED

- [x] src/server.ts contains unhandledRejection handler
- [x] src/server.ts contains uncaughtException handler
- [x] src/server.ts has 3 try/catch blocks (POST + GET + DELETE)
- [x] src/db/index.ts has idle_timeout: 0
- [x] src/db/index.ts has connect_timeout: 10
- [x] docker-compose.prod.yml has restart: unless-stopped
- [x] TypeScript compiles without errors
- [x] Commits bd9efde and 83749fb exist
