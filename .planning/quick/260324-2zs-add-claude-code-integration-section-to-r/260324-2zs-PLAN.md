---
phase: quick
plan: 260324-2zs
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [quick-task]
must_haves:
  truths:
    - "README has a 'Integrating with Claude Code' section between 'Connect to Claude Code' (step 4) and 'Use the MCP Inspector' (step 5)"
    - "Section contains a ready-to-paste CLAUDE.md snippet with tool names, behavioral instructions, and presentation format"
    - "Section contains a hooks subsection showing the Stop hook for session-end memory review"
    - "CLAUDE.md snippet uses the project's actual tool names (memory_create, memory_search, etc.) not aliases"
  artifacts:
    - path: "README.md"
      provides: "Claude Code integration documentation"
      contains: "Integrating with Claude Code"
  key_links: []
---

<objective>
Add a new "Integrating with Claude Code" section to README.md that teaches users how to:
1. Add a CLAUDE.md snippet to their project so Claude Code knows how to use Agentic Brain tools
2. Set up a Stop hook for automated session-end memory review

Purpose: Users who install the MCP server (step 4) still need behavioral instructions in their CLAUDE.md so Claude Code actually calls the tools at the right times. This section bridges that gap.

Output: Updated README.md with the new section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@docs/hooks/README.md
@docs/hooks/memory-session-review.sh
@docs/hooks/settings-snippet.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Integrating with Claude Code" section to README</name>
  <files>README.md</files>
  <action>
Insert a new section after the "### 4. Connect to Claude Code" block (after the closing of the MCP settings JSON snippet around line 131) and before "### 5. Use the MCP Inspector". The new content should be:

### 5. Integrate with Claude Code (optional)

Two optional enhancements that make Claude Code work better with Agentic Brain.

#### Add instructions to your CLAUDE.md

Create or edit `CLAUDE.md` in your project root (or `~/.claude/CLAUDE.md` for global) and paste this snippet. It tells Claude Code when and how to use the memory tools:

```markdown
## Agent Memory

This project uses [Agentic Brain](https://github.com/TODO/agent-brain) for shared team knowledge.

### Available Tools

- **memory_search** -- Search for relevant memories. Call with a query describing what you need.
- **memory_create** -- Save a new memory from important context the user shares.
- **memory_get** -- Read a specific memory by ID.
- **memory_update** -- Modify an existing memory.
- **memory_comment** -- Append a comment to an existing memory (turns it into a thread).
- **memory_verify** -- Confirm a memory is still accurate (updates verified_at).
- **memory_archive** -- Archive a memory that is no longer relevant.
- **memory_list_stale** -- List memories that need review (old or unverified).

### When to Call `memory_search`

**Call `memory_search` before actions that affect shared systems.** This includes:

1. **The user asks about notes, context, or team knowledge** -- e.g. "any notes?", "what should I know?"
2. **Before actions that affect shared infrastructure** -- deploys, database migrations, credential rotation, etc.
3. **Before running shared/integration tests** (e.g. E2E, load tests) -- but NOT local unit tests or builds

**Do NOT search for purely local actions** like editing files, installing dependencies, running local builds, linting, or formatting.

### When the User Shares Important Context

If the user mentions decisions, temporary changes, or gotchas that the team should know about, suggest saving a memory with `memory_create`. Always confirm before saving.

### Presenting Memories

Always **number** memories and include **author**, **date**, and **title**. The user may refer to memories by number (e.g. "archive memory 2", "comment on 1").
```

Adapt the snippet from the user's global CLAUDE.md at `~/.claude/CLAUDE.md` (the `## Agent Memory` section) but change it to use the project's actual tool names: `memory_search` instead of `search_memory`, `memory_create` instead of `save_note`, `memory_get` instead of `get_note`, `memory_update` instead of `update_note`, `memory_comment` instead of `comment_note`, `memory_verify` instead of `verify_note`, `memory_archive` instead of `archive_note`, `memory_list_stale` instead of `list_stale`. Keep it concise -- omit the "How to call" min_relevance detail and the guardrails/note-presentation formatting details to keep the snippet practical and not overwhelming. Include the key behavioral triggers (when to search, when to save).

#### Add a session-review hook (optional)

A Stop hook can remind Claude to review and save learnings before ending a session.

**Prerequisites:** `jq` installed (`brew install jq` on macOS).

**Step 1:** Copy the hook script:

```bash
mkdir -p .claude/hooks
cp docs/hooks/memory-session-review.sh .claude/hooks/
chmod +x .claude/hooks/memory-session-review.sh
```

**Step 2:** Add to `.claude/settings.json`:

```json
{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/memory-session-review.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
```

When Claude is about to stop, the hook blocks the first attempt and asks Claude to reflect on the session and save key learnings. See `docs/hooks/README.md` for details.

---

IMPORTANT: Renumber the existing "### 5. Use the MCP Inspector" to "### 6. Use the MCP Inspector" since this new section takes the number 5 slot.

Do NOT duplicate or modify the existing step 4 MCP settings content -- this section is purely additive.
</action>
<verify>
<automated>grep -n "Integrating with Claude Code\|Integrate with Claude Code\|Add instructions to your CLAUDE.md\|session-review hook\|memory_search.\*Search for relevant\|### 6. Use the MCP Inspector" README.md | head -20</automated>
</verify>
<done> - README.md contains a new "Integrate with Claude Code" section between steps 4 and 6 - Section includes a ready-to-paste CLAUDE.md snippet with correct tool names (memory_search, memory_create, etc.) - Section includes hook setup instructions referencing docs/hooks/ - Former step 5 is now step 6
</done>
</task>

</tasks>

<verification>
- `grep -c "Integrate with Claude Code" README.md` returns 1
- `grep -c "memory_session_review\|memory-session-review" README.md` returns at least 1 (hook reference)
- `grep "### 6" README.md` shows the MCP Inspector is now step 6
- The CLAUDE.md snippet inside README uses `memory_search` (not `search_memory`) and `memory_create` (not `save_note`)
</verification>

<success_criteria>
README.md has a clear, self-contained "Integrate with Claude Code" section that a new user can follow to set up CLAUDE.md instructions and the session-review hook without referencing any other documentation.
</success_criteria>

<output>
After completion, create `.planning/quick/260324-2zs-add-claude-code-integration-section-to-r/260324-2zs-SUMMARY.md`
</output>
