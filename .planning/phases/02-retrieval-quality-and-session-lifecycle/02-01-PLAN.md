---
phase: 02-retrieval-quality-and-session-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/scoring.ts
  - src/types/memory.ts
  - src/config.ts
  - src/repositories/types.ts
  - src/repositories/memory-repository.ts
  - src/services/memory-service.ts
  - tests/unit/scoring.test.ts
autonomous: true
requirements:
  - RETR-02
  - RETR-03

must_haves:
  truths:
    - "computeRelevance() produces a composite score in [0, 1] combining similarity, recency, and verification boost"
    - "exponentialDecay() returns 1.0 for age=0, 0.5 for age=halfLife, approaches 0 for large ages"
    - "MemoryWithScore is renamed to MemoryWithRelevance with a 'relevance' field -- ONE type, not two"
    - "All repository, service, and type imports referencing MemoryWithScore are updated to MemoryWithRelevance"
    - "Config loads RECENCY_HALF_LIFE_DAYS from env with default 14"
  artifacts:
    - path: "src/utils/scoring.ts"
      provides: "Pure scoring functions for composite relevance"
      exports: ["computeRelevance", "exponentialDecay", "SIMILARITY_WEIGHT", "RECENCY_WEIGHT", "VERIFICATION_BOOST", "OVER_FETCH_FACTOR"]
    - path: "src/types/memory.ts"
      provides: "MemoryWithRelevance type (renamed from MemoryWithScore)"
      contains: "MemoryWithRelevance"
    - path: "src/config.ts"
      provides: "Recency half-life configuration"
      contains: "recencyHalfLifeDays"
    - path: "tests/unit/scoring.test.ts"
      provides: "Unit tests for scoring functions"
      min_lines: 60
  key_links:
    - from: "src/utils/scoring.ts"
      to: "src/config.ts"
      via: "half-life config value used as parameter"
      pattern: "halfLifeDays"
    - from: "src/types/memory.ts"
      to: "src/utils/scoring.ts"
      via: "MemoryWithRelevance is the output shape of scored results"
      pattern: "MemoryWithRelevance"
    - from: "src/repositories/types.ts"
      to: "src/types/memory.ts"
      via: "imports MemoryWithRelevance (was MemoryWithScore)"
      pattern: "MemoryWithRelevance"
---

<objective>
Create the scoring foundation for composite relevance ranking and rename MemoryWithScore to MemoryWithRelevance across the entire codebase.

Purpose: All subsequent plans depend on the scoring functions and updated types. This plan establishes the mathematical foundation (exponential decay, composite scoring) and performs the D-06 type rename (MemoryWithScore -> MemoryWithRelevance, similarity -> relevance) with cascading updates to all references in repository, service, and type layers.

Output: Pure scoring functions with unit tests, renamed memory type, config extension for recency half-life.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types/memory.ts
@src/config.ts
@src/repositories/types.ts
@src/repositories/memory-repository.ts
@src/services/memory-service.ts
@tests/helpers.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scoring functions and unit tests</name>
  <files>src/utils/scoring.ts, tests/unit/scoring.test.ts</files>
  <read_first>
    - src/types/memory.ts (current MemoryWithScore type)
    - src/config.ts (config pattern to follow)
    - tests/helpers.ts (test infrastructure patterns)
  </read_first>
  <behavior>
    - exponentialDecay(0, 14) returns 1.0 (brand new memory)
    - exponentialDecay(14, 14) returns 0.5 (one half-life elapsed)
    - exponentialDecay(28, 14) returns 0.25 (two half-lives)
    - exponentialDecay(-1, 14) returns 1.0 (negative age clamped)
    - computeRelevance(1.0, now, null, 14) returns ~0.95 (perfect similarity, brand new, not verified)
    - computeRelevance(1.0, now, now, 14) returns 1.0 (perfect similarity, brand new, verified)
    - computeRelevance(0.5, 14-day-old-date, null, 14) returns ~0.475 (0.80*0.5 + 0.15*0.5 = 0.475)
    - computeRelevance(1.0, 1000-day-old-date, null, 14) returns ~0.80 (similarity dominates for old memories)
    - All scores are clamped to [0, 1]
    - OVER_FETCH_FACTOR is exported as 3
  </behavior>
  <action>
    Create `src/utils/scoring.ts` with these exports (per D-01, D-02, D-03, D-04):

    ```typescript
    export const SIMILARITY_WEIGHT = 0.80;
    export const RECENCY_WEIGHT = 0.15;
    export const VERIFICATION_BOOST = 0.05;
    export const OVER_FETCH_FACTOR = 3;

    export function exponentialDecay(ageDays: number, halfLifeDays: number): number
    export function computeRelevance(
      similarity: number,
      createdAt: Date,
      verifiedAt: Date | null,
      halfLifeDays: number,
      now?: Date,
    ): number
    ```

    Formula per D-01/D-02/D-03:
    - `recencyDecay = Math.pow(0.5, ageDays / halfLifeDays)` where `ageDays = max(0, (now - createdAt) / ms_per_day)`
    - `relevance = (0.80 * similarity) + (0.15 * recencyDecay) + (verifiedAt !== null ? 0.05 : 0)`
    - Clamp result to `Math.min(1, Math.max(0, relevance))`
    - If `ageDays <= 0`, treat recencyDecay as 1.0

    Create `tests/unit/scoring.test.ts` with vitest. Test the exact behaviors listed above. Use explicit Date construction for deterministic tests (e.g., `new Date('2026-03-23T00:00:00Z')` as "now" and subtract days for createdAt). Do NOT use `Date.now()` in tests -- always pass an explicit `now` parameter.

    TDD: Write tests first, run them (expect RED), then implement, run again (expect GREEN).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/scoring.test.ts -x</automated>
  </verify>
  <done>All scoring unit tests pass. exponentialDecay and computeRelevance are exported and tested with at least 8 test cases covering brand-new, half-life, old memories, verified vs unverified, and clamping. src/utils/scoring.ts contains all exported constants and functions. `npx vitest run tests/unit/scoring.test.ts -x` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Rename MemoryWithScore to MemoryWithRelevance and extend config</name>
  <files>src/types/memory.ts, src/config.ts, src/repositories/types.ts, src/repositories/memory-repository.ts, src/services/memory-service.ts</files>
  <read_first>
    - src/types/memory.ts (current MemoryWithScore definition)
    - src/config.ts (current config shape)
    - src/repositories/types.ts (imports MemoryWithScore)
    - src/repositories/memory-repository.ts (imports and uses MemoryWithScore)
    - src/services/memory-service.ts (imports and uses MemoryWithScore)
  </read_first>
  <action>
    **CRITICAL: Per D-06, this is a RENAME, not an addition. There is ONE type, not two. The user has explicitly confirmed this.**

    **Type rename (per D-05, D-06):**

    In `src/types/memory.ts`:
    1. Rename `MemoryWithScore` to `MemoryWithRelevance`
    2. Rename the `similarity: number` field to `relevance: number`
    3. Update the comment to reference D-05/D-06
    4. The result should be:
       ```typescript
       // D-05, D-06: Search result with relevance score (renamed from MemoryWithScore/similarity)
       export interface MemoryWithRelevance extends Memory {
         relevance: number;
       }
       ```
    5. `MemoryWithScore` must NOT exist anywhere in memory.ts after this change

    **Cascade updates -- ALL files that reference MemoryWithScore must be updated:**

    In `src/repositories/types.ts`:
    1. Change import: `MemoryWithScore` -> `MemoryWithRelevance`
    2. Change `search()` return type: `Promise<MemoryWithScore[]>` -> `Promise<MemoryWithRelevance[]>`

    In `src/repositories/memory-repository.ts`:
    1. Change import: `MemoryWithScore` -> `MemoryWithRelevance`
    2. Change `search()` return type: `Promise<MemoryWithScore[]>` -> `Promise<MemoryWithRelevance[]>`
    3. In the search method's row mapping, change the property name from `similarity` to `relevance`:
       Where it currently maps `similarity: Number(...)` from the cosine distance calculation, rename to `relevance: Number(...)`.
       (Note: at this point in the pipeline, this value is still raw cosine similarity. Plan 02 will change the service layer to compute composite relevance and overwrite this value. The rename is correct per D-06 -- the field is called `relevance` throughout the type system.)

    In `src/services/memory-service.ts`:
    1. Change import: `MemoryWithScore` -> `MemoryWithRelevance`
    2. Change `search()` return type: `Promise<Envelope<MemoryWithScore[]>>` -> `Promise<Envelope<MemoryWithRelevance[]>>`

    **Config changes (per D-07):**

    In `src/config.ts`, add `recencyHalfLifeDays` field:
    ```typescript
    recencyHalfLifeDays: Number(process.env.RECENCY_HALF_LIFE_DAYS ?? "14"),
    ```

    **Verification after rename:** Run `grep -r "MemoryWithScore" src/` to confirm ZERO matches. Every reference must now be `MemoryWithRelevance`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>MemoryWithScore is fully renamed to MemoryWithRelevance across the codebase. `grep -r "MemoryWithScore" src/` returns zero matches. The type has `relevance: number` (not `similarity`). All imports in repositories/types.ts, memory-repository.ts, and memory-service.ts reference MemoryWithRelevance. Config includes recencyHalfLifeDays with env var and default 14. `npx tsc --noEmit` exits 0.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/scoring.test.ts -x` -- all scoring unit tests pass
- `npx tsc --noEmit` -- no type errors after type rename and config extension
- `grep -r "MemoryWithRelevance" src/types/memory.ts` -- type exists
- `grep -r "MemoryWithScore" src/` -- returns ZERO matches (complete rename)
- `grep "recencyHalfLifeDays" src/config.ts` -- config field exists
- `grep "OVER_FETCH_FACTOR" src/utils/scoring.ts` -- constant exported
</verification>

<success_criteria>
- Scoring functions (computeRelevance, exponentialDecay) are implemented, exported, and tested with 8+ unit tests
- MemoryWithScore is RENAMED to MemoryWithRelevance with `relevance: number` field (ONE type, not two)
- All 4 files that referenced MemoryWithScore are updated: memory.ts, repositories/types.ts, memory-repository.ts, memory-service.ts
- `grep -r "MemoryWithScore" src/` returns zero matches
- Config loads RECENCY_HALF_LIFE_DAYS with default 14
- TypeScript compiles cleanly
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-retrieval-quality-and-session-lifecycle/02-01-SUMMARY.md`
</output>
