---
phase: 04-agent-autonomy
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - tests/unit/budget.test.ts
  - tests/unit/dedup.test.ts
  - tests/integration/session-lifecycle.test.ts
  - tests/integration/duplicate-detection.test.ts
  - tests/integration/prompt-resource.test.ts
autonomous: true
requirements: [AUTO-01, AUTO-02, AUTO-03, AUTO-04, AUTO-05]

must_haves:
  truths:
    - "Unit tests verify budget threshold logic and dedup scope logic in isolation"
    - "Integration tests verify session_id generation, budget tracking through full create flow, and duplicate detection with real database"
    - "Integration tests verify MCP prompt resource registration and content"
    - "All 5 AUTO requirements have at least one test covering the behavior"
  artifacts:
    - path: "tests/unit/budget.test.ts"
      provides: "Budget threshold and counting logic unit tests"
      contains: "describe"
    - path: "tests/unit/dedup.test.ts"
      provides: "Dedup threshold comparison and scope logic unit tests"
      contains: "describe"
    - path: "tests/integration/session-lifecycle.test.ts"
      provides: "Session ID, budget tracking, autonomous write integration tests"
      contains: "session_id"
    - path: "tests/integration/duplicate-detection.test.ts"
      provides: "Semantic duplicate detection integration tests"
      contains: "duplicate"
    - path: "tests/integration/prompt-resource.test.ts"
      provides: "MCP prompt resource registration test"
      contains: "memory-guidance"
  key_links:
    - from: "tests/integration/session-lifecycle.test.ts"
      to: "src/services/memory-service.ts"
      via: "create() and sessionStart() calls"
      pattern: "memoryService"
    - from: "tests/integration/duplicate-detection.test.ts"
      to: "src/repositories/memory-repository.ts"
      via: "findDuplicates() and create() calls"
      pattern: "findDuplicates"
---

<objective>
Write comprehensive unit and integration tests covering all Phase 4 features: session lifecycle, write budget, duplicate detection, and prompt resource.

Purpose: These tests verify the five AUTO requirements are correctly implemented and serve as regression protection. The research identified specific testing pitfalls (mock embedding sensitivity, dedup scope logic) that the tests must account for.

Output: Green test suite covering all Phase 4 behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-agent-autonomy/04-CONTEXT.md
@.planning/phases/04-agent-autonomy/04-RESEARCH.md
@.planning/phases/04-agent-autonomy/04-01-SUMMARY.md
@.planning/phases/04-agent-autonomy/04-03-SUMMARY.md

<interfaces>
<!-- Contracts the test executor needs to understand -->

From src/services/memory-service.ts (after Plans 01+03):

```typescript
class MemoryService {
  constructor(
    memoryRepo,
    projectRepo,
    embeddingProvider,
    commentRepo?,
    sessionRepo?,
    sessionLifecycleRepo?,
  );
  async create(
    input: MemoryCreate,
  ): Promise<Envelope<Memory | CreateSkipResult>>;
  async sessionStart(
    projectId,
    userId,
    context?,
    limit?,
  ): Promise<Envelope<MemoryWithRelevance[]>>;
  // sessionStart response meta includes session_id
}
```

From src/types/memory.ts (after Plan 01):

```typescript
export interface CreateSkipResult {
  skipped: true;
  reason: "budget_exceeded" | "duplicate";
  message: string;
  duplicate?: { id: string; title: string; relevance: number; scope?: string };
}
```

From src/repositories/types.ts (after Plans 01+03):

```typescript
export interface SessionRepository {
  createSession(id: string, userId: string, projectId: string): Promise<void>;
  getBudget(sessionId: string): Promise<{ used: number; limit: number } | null>;
  incrementBudgetUsed(sessionId: string, limit: number): Promise<{ used: number; exceeded: boolean }>;
  findById(sessionId: string): Promise<...>;
}

// MemoryRepository now includes:
findDuplicates(options: { embedding, projectId, scope, userId, threshold }): Promise<...>
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for budget logic and dedup threshold logic</name>
  <files>tests/unit/budget.test.ts, tests/unit/dedup.test.ts</files>
  <read_first>
    - src/services/memory-service.ts
    - src/config.ts
    - src/types/memory.ts
    - src/repositories/types.ts
    - tests/unit/ (list existing test files for pattern reference)
  </read_first>
  <action>
**tests/unit/budget.test.ts** -- Test the budget enforcement logic using mocked repositories. Use vitest. Create a MemoryService with mocked repos to test:

1. `"autonomous write with source agent-auto succeeds when under budget"` -- mock sessionLifecycleRepo.getBudget returning `{ used: 5, limit: 10 }`, mock incrementBudgetUsed returning `{ used: 6, exceeded: false }`. Call create with `source: 'agent-auto', session_id: 'test-session'`. Assert result.data has an `id` property (not skipped).

2. `"autonomous write with source session-review succeeds when under budget"` -- same setup but `source: 'session-review'`. Assert success.

3. `"autonomous write is soft-rejected when budget exceeded"` -- mock getBudget returning `{ used: 10, limit: 10 }`. Call create with `source: 'agent-auto', session_id: 'test-session'`. Assert result.data.skipped === true, result.data.reason === 'budget_exceeded', result.meta.budget.exceeded === true.

4. `"manual write bypasses budget check"` -- mock getBudget to throw (should never be called). Call create with `source: 'manual'`. Assert result.data has `id` (not skipped).

5. `"autonomous write without session_id throws ValidationError"` -- Call create with `source: 'agent-auto'` and no session_id. Assert throws ValidationError with message containing 'session_id is required'.

6. `"manual write without session_id succeeds"` -- Call create with `source: 'manual'` and no session_id. Assert success.

Mock setup pattern: create mock memoryRepo with `create` returning a Memory object, mock projectRepo with `findOrCreate`, mock embeddingProvider with `embed` returning a 512-dim zero vector, mock sessionLifecycleRepo with the relevant methods.

**tests/unit/dedup.test.ts** -- Test dedup logic in memory_create using mocked findDuplicates:

1. `"create is soft-rejected when duplicate found"` -- mock memoryRepo.findDuplicates returning `[{ id: 'existing-1', title: 'Existing', relevance: 0.95, scope: 'project' }]`. Call create. Assert result.data.skipped === true, result.data.reason === 'duplicate', result.data.duplicate.id === 'existing-1'.

2. `"create succeeds when no duplicate found"` -- mock findDuplicates returning `[]`. Call create. Assert result.data has `id` property.

3. `"duplicate detection runs on manual writes too"` -- per D-14. Mock findDuplicates returning a match. Call create with `source: 'manual'`. Assert result.data.skipped === true.

4. `"cross-scope duplicate message for user memory matching project"` -- mock findDuplicates returning `[{ id: 'proj-1', title: 'Project Memory', relevance: 0.92, scope: 'project' }]`. Call create with `scope: 'user'`. Assert result.data.message contains 'shared knowledge'.

For mocks: use `vi.fn()` for each method. The embeddingProvider mock should return consistent 512-dim vectors. For dedup tests, the key is that findDuplicates returns or doesn't return results -- the actual cosine math is tested in integration tests.
</action>
<verify>
<automated>cd /Users/chris/dev/agent-brain && npx vitest run tests/unit/budget.test.ts tests/unit/dedup.test.ts --reporter=verbose 2>&1 | tail -30</automated>
</verify>
<acceptance_criteria> - tests/unit/budget.test.ts exists and contains at least 6 test cases - tests/unit/budget.test.ts contains `budget_exceeded` assertion - tests/unit/budget.test.ts contains `ValidationError` assertion for missing session_id - tests/unit/budget.test.ts contains test for `source: 'manual'` bypassing budget - tests/unit/dedup.test.ts exists and contains at least 4 test cases - tests/unit/dedup.test.ts contains `reason === 'duplicate'` assertion - tests/unit/dedup.test.ts contains test for manual writes triggering dedup (D-14) - tests/unit/dedup.test.ts contains `shared knowledge` message assertion - Both test files pass: `npx vitest run tests/unit/budget.test.ts tests/unit/dedup.test.ts` exits 0
</acceptance_criteria>
<done>Unit tests verify budget enforcement (under budget, exceeded, manual bypass, missing session_id) and dedup logic (detected, not detected, manual writes, cross-scope messaging) with mocked repositories.</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for session lifecycle, dedup, and prompt resource</name>
  <files>tests/integration/session-lifecycle.test.ts, tests/integration/duplicate-detection.test.ts, tests/integration/prompt-resource.test.ts</files>
  <read_first>
    - tests/integration/ (list existing test files for pattern reference -- specifically database setup, test helpers)
    - src/services/memory-service.ts
    - src/repositories/session-repository.ts
    - src/repositories/memory-repository.ts
    - src/prompts/memory-guidance.ts
    - src/db/schema.ts
    - src/config.ts
  </read_first>
  <action>
Follow existing integration test patterns from prior phases (Docker Postgres, Drizzle, real repositories). Check existing integration tests for the setup/teardown pattern -- likely uses `beforeAll` to create db connection and run migrations, `afterAll` to close, and `beforeEach` to truncate tables.

**tests/integration/session-lifecycle.test.ts** -- Tests covering AUTO-01, AUTO-03, AUTO-04:

1. `"memory_session_start returns session_id in meta"` -- Call sessionStart(). Assert result.meta.session_id is a string with length 21 (nanoid).

2. `"autonomous write with valid session_id succeeds and tracks budget"` -- Call sessionStart to get session_id. Call create with `source: 'agent-auto', session_id`. Assert success. Assert result.meta.budget.used === 1.

3. `"session-review write counts toward budget"` -- Create with `source: 'session-review', session_id`. Assert meta.budget.used increments.

4. `"budget enforcement after limit reached"` -- Loop create with `source: 'agent-auto'` until budget.used reaches config.writeBudgetPerSession (10). Assert the 11th call returns `skipped: true, reason: 'budget_exceeded'`.

5. `"manual write does not count toward budget"` -- After some autonomous writes, create with `source: 'manual', session_id`. Assert budget.used does not increment (check by doing another autonomous write and verifying budget.used is same as before manual write + 1).

6. `"autonomous write without session_id is rejected"` -- Create with `source: 'agent-auto'`, no session_id. Assert throws/rejects with error containing 'session_id is required'.

**tests/integration/duplicate-detection.test.ts** -- Tests covering AUTO-05:

1. `"identical content is detected as duplicate"` -- Create a memory with content "X". Try to create another memory with identical content "X". Assert second call returns `skipped: true, reason: 'duplicate'`, and `duplicate.id` matches the first memory's id.

2. `"completely different content passes dedup"` -- Create memory "A completely unique insight about databases". Create memory "A totally different observation about testing". Assert second succeeds.

3. `"project-scoped dedup checks project scope only"` -- Create a user-scoped memory. Try to create a project-scoped memory with identical content. Assert the project memory is NOT flagged as duplicate (project dedup only checks project scope, not user scope).

4. `"user-scoped dedup checks both user and project scope"` (D-16) -- Create a project-scoped memory. Try to create a user-scoped memory with identical content. Assert it IS flagged as duplicate with the project memory's info. Assert message contains 'shared knowledge'.

5. `"dedup response includes existing memory info"` -- Create original, try duplicate. Assert `duplicate.id`, `duplicate.title`, and `duplicate.relevance` are present and reasonable.

6. `"archived memories are excluded from dedup check"` -- Create a memory, archive it. Try to create another with identical content. Assert success (not flagged as duplicate).

Per Pitfall 6 from RESEARCH: use identical content for dedup detection tests (mock embedding is hash-based, so identical text produces identical vectors with similarity 1.0). For "different content" tests, use substantially different text.

**tests/integration/prompt-resource.test.ts** -- Test covering AUTO-02:

1. `"memory-guidance prompt is registered and returns content"` -- Create McpServer instance, call registerMemoryGuidance(server). Use server's internal prompt listing to verify the prompt exists. Invoke the prompt and verify it returns messages with role 'user' and text content containing 'What to Capture', 'When to Save', 'Session-End Review'.

If directly invoking prompts on the server is complex, alternatively: import `registerMemoryGuidance` and `MEMORY_GUIDANCE_TEXT` (export it for testing), verify the text contains required sections. This is simpler and equally valid since the MCP SDK's registerPrompt is tested by the SDK itself.
</action>
<verify>
<automated>cd /Users/chris/dev/agent-brain && npx vitest run tests/integration/session-lifecycle.test.ts tests/integration/duplicate-detection.test.ts tests/integration/prompt-resource.test.ts --reporter=verbose 2>&1 | tail -40</automated>
</verify>
<acceptance_criteria> - tests/integration/session-lifecycle.test.ts exists with at least 6 test cases - tests/integration/session-lifecycle.test.ts contains test for session_id in meta - tests/integration/session-lifecycle.test.ts contains test for budget enforcement at limit - tests/integration/session-lifecycle.test.ts contains test for manual write not counting toward budget - tests/integration/duplicate-detection.test.ts exists with at least 6 test cases - tests/integration/duplicate-detection.test.ts contains test for identical content detection - tests/integration/duplicate-detection.test.ts contains test for user-scoped dedup checking project scope (D-16) - tests/integration/duplicate-detection.test.ts contains test for archived memories exclusion - tests/integration/prompt-resource.test.ts exists with at least 1 test case - tests/integration/prompt-resource.test.ts contains assertion for 'What to Capture' or 'memory-guidance' - All integration tests pass: `npx vitest run tests/integration/ --reporter=verbose` exits 0 - Full test suite passes: `npx vitest run` exits 0
</acceptance_criteria>
<done>Integration tests verify session_id generation, budget tracking through 10+ writes, manual bypass, dedup with identical/different content, scope-aware dedup (D-16), archived exclusion, and prompt resource registration. Full test suite green.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` -- full suite green
2. `npx vitest run tests/unit/budget.test.ts` -- budget unit tests pass
3. `npx vitest run tests/unit/dedup.test.ts` -- dedup unit tests pass
4. `npx vitest run tests/integration/session-lifecycle.test.ts` -- session integration tests pass
5. `npx vitest run tests/integration/duplicate-detection.test.ts` -- dedup integration tests pass
6. `npx vitest run tests/integration/prompt-resource.test.ts` -- prompt resource test passes
</verification>

<success_criteria>

- All 5 AUTO requirements have at least one test
- Unit tests verify budget and dedup logic with mocked dependencies
- Integration tests verify full flow with real database
- Budget enforcement is tested at the exact limit boundary (write 10, attempt 11th)
- Scope-aware dedup is tested for both project and user scopes
- Archived memories are excluded from dedup checks
- Prompt resource content verified to contain required sections
- Full test suite passes: `npx vitest run` exits 0
  </success_criteria>

<output>
After completion, create `.planning/phases/04-agent-autonomy/04-04-SUMMARY.md`
</output>
